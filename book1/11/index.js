let flag = true;

if (flag) {
    // Module parse failed: 'import' and 'export' may only appear at the top level (4:4)
    // import { name, add, obj } from './demo.js';

    // ES Modules 语法不支持, CommonJS 支持
    // const obj = require('./demo.js');
    // console.log(obj);
}


